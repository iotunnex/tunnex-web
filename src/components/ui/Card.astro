---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class={`t-card rounded-[14px] border border-border bg-surface shadow-[0_10px_30px_rgba(0,0,0,0.3)] p-6 ${className}`}
>
  <slot />
</div>
